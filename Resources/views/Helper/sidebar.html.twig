<aside id="sidebar" class="sidebar c-overflow">
    <div class="profile-menu">
        <a href="">
            <div class="profile-pic">
                <img src="{{ asset('bundles/fly/images/profile/4.jpg')  }}" alt="">
            </div>

            <div class="profile-info">
                {{ app.security.token.user.name }}

                <i class="zmdi zmdi-caret-down"></i>
            </div>
        </a>

        <ul class="main-menu">
            <li>
                <a href="{{ path('fly_user_edit', { 'id': app.security.token.user.id }) }}"><i class="zmdi zmdi-settings"></i> Configurações</a>
            </li>
            <li>
                <a href="{{ path('cms_logout') }}"><i class="zmdi zmdi-time-restore"></i> Sair</a>
            </li>
        </ul>
    </div>
    <ul class="main-menu">
        <li {% if configs.prefix_route == 'fly_dashboard' %} class="active"{% endif %}>
            <a href="{{ path('fly_dashboard') }}" {% if configs.prefix_route == 'fly_dashboard' %} class="active"{% endif %}>
                <i class="zmdi zmdi-home"></i> Inicio
            </a>
        </li>
        {{ render(controller('FlyBundle:Menu:menu', {"active": routeBase, "activeParams": viewDefaultParams })) }}
    </ul>
</aside>